# Required Vercel environment variables.
# Set these in your Vercel project: Settings -> Environment Variables.

# Your Anthropic API key (used by /api/generate).
ANTHROPIC_API_KEY=

# A single shared password that gates the landing page.
ACCESS_PASSWORD=
